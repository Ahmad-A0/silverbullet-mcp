{
  "name": "silverbullet-mcp-server",
  "version": "1.2.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "npm run build && node --test test/*.test.cjs test/regressions/*.test.cjs",
    "test:regressions": "npm run build && node --test test/regressions/*.test.cjs",
    "test:live": "npm run build && node --test test/live/*.test.cjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "diff": "^9.0.0",
    "express": "^4.22.3",
    "zod": "^3.20.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^20.0.0",
    "ajv": "^8.20.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
